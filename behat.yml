# behat.yml
default:
    paths:
        features: 'features'
        bootstrap: 'features/bootstrap' 
    context:
        parameters:
            base_url: http://api.dnanotifications.dev
            environment: local